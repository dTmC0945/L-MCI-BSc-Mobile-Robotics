#include <iostream>
#include <vector>

int main() {
    std::vector<int> vi(6); // vector of 6 integers initialised to 0
    std::vector<std::string> vs(4); // vector of 4 strings initialised to ""

}
